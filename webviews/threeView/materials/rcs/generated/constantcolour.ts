/**
 * GENERATED by scripts/gen-materials.ts from
 *   data/materials/billboards/constantcolour.rcsmaterial
 *
 * 2 permutations in the file; 2 draw something, reduced to
 * 1 distinct (vertex-stream, sampler) signatures -- the only thing a mesh
 * can be matched against, since the engine's own choice depends on render state
 * the viewer does not model. Do not edit: regenerate instead.
 */
import * as THREE from "three";
import { MaterialFactory } from "../_abstract";
import { Permutation } from "../_raw";
import { GeneratedRcsMaterial } from "../_generated";
import { makeVariant, pickVariant, GeneratedVariant } from "../_variant";
import { S_ec007f00ed91, S_fc474b9c0008 } from "./_shaders";

const VARIANTS: GeneratedVariant[] = [
  {
    material: "constantcolour.rcsmaterial",
    permutation: 0,
    backend: "Static",
    vert: S_fc474b9c0008,
    frag: S_ec007f00ed91,
    attributes: [{ id: 570870450, reg: 2, name: "VertexColour" }, { id: 3117619978, reg: 0, name: "position" }, { id: 3732576027, reg: 1, name: "normal" }],
    samplers: [],
    bank: [{ slot: 465, name: "eyePositionWorldSpace", rows: 1 }, { slot: 466, name: "positionScale", rows: 1 }, { slot: 467, name: "positionBias", rows: 1 }],
    bankWindow: null,
    bankDefaults: [],
  },
];

export const constantcolour_gen: MaterialFactory = {
  name: "constantcolour.rcsmaterial",
  minTextures: 1,
  maxTextures: 8,
  make: (textures: THREE.Texture[]) => makeVariant(VARIANTS[VARIANTS.length - 1], new Map(), textures),
  makeById: (channels: Map<number, THREE.Texture>, streams?: Set<number>) =>
    makeVariant(pickVariant(VARIANTS, channels, streams), channels, []),
};

export const constantcolour_VARIANTS = VARIANTS;
