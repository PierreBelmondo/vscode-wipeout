/**
 * GENERATED by scripts/gen-materials.ts from
 *   data/ribboneffects/materials/hd_enginetrail_bluered.rcsmaterial
 *
 * 1 permutations in the file; 1 draw something, reduced to
 * 1 distinct (vertex-stream, sampler) signatures -- the only thing a mesh
 * can be matched against, since the engine's own choice depends on render state
 * the viewer does not model. Do not edit: regenerate instead.
 */
import * as THREE from "three";
import { MaterialFactory } from "../_abstract";
import { Permutation } from "../_raw";
import { GeneratedRcsMaterial } from "../_generated";
import { makeVariant, pickVariant, GeneratedVariant } from "../_variant";
import { S_709148af8c3a, S_fb59be8643cb } from "./_shaders";

const VARIANTS: GeneratedVariant[] = [
  {
    material: "hd_enginetrail_bluered.rcsmaterial",
    permutation: 0,
    backend: "?",
    vert: S_709148af8c3a,
    frag: S_fb59be8643cb,
    attributes: [{ id: 1114772732, reg: 2, name: "Uv1" }, { id: 1955845200, reg: 3, name: "VertexColour1" }, { id: 3117619978, reg: 0, name: "position" }, { id: 3732576027, reg: 1, name: "normal" }],
    samplers: [{ id: 658712875, unit: 1, name: "?" }, { id: 3428562983, unit: 0, name: "?" }],
    bank: [{ slot: 466, name: "TrailSpeed", rows: 1 }, { slot: 467, name: "eyePositionWorldSpace", rows: 1 }],
    bankWindow: null,
    bankDefaults: [],
  },
];

export const hd_enginetrail_bluered_gen: MaterialFactory = {
  name: "hd_enginetrail_bluered.rcsmaterial",
  minTextures: 1,
  maxTextures: 8,
  make: (textures: THREE.Texture[]) => makeVariant(VARIANTS[VARIANTS.length - 1], new Map(), textures),
  makeById: (channels: Map<number, THREE.Texture>, streams?: Set<number>) =>
    makeVariant(pickVariant(VARIANTS, channels, streams), channels, []),
};

export const hd_enginetrail_bluered_VARIANTS = VARIANTS;
